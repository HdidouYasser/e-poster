/**
 * useTotemApi.js
 * 
 * Note: Les anciens hooks obsolètes faisant appel à des endpoints inexistants ont été nettoyés.
 * Les requêtes vers l'API Totem publique ont été consolidées directement dans les composants
 * correspondants (TotemHome, TotemPublications, TotemPosterDetail) pour un comportement dynamique
 * et un typage direct via react-query.
 */
export {};
